import BookingSettingsPage from '@/components/booking/BookingSettingsPage';

export const metadata = { title: 'SEDO Bookings — RPJ Corp' };

export default function SedoBookingsPage() {
  return <BookingSettingsPage />;
}
