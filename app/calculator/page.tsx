import EcomCalculator from '@/components/calculator/EcomCalculator';

export const metadata = { title: 'Ecom Calculator — RPJ Corp' };

export default function CalculatorPage() {
  return <EcomCalculator />;
}
