import BookingPage from '@/components/booking/BookingPage';

export const metadata = { title: 'Book a Discovery Call — RPJ Corp' };

export default function DiscoveryCallBookingPage() {
  return <BookingPage />;
}
